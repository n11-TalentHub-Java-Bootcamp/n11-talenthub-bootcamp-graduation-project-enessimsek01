# n11-talenthub-bootcamp-graduation-project

This is a Credit Application with using Spring Framework and PostgreSQL

The purpose of this application is calculating the credit limit for each user

>NOTE: **You should provide your url and password which is used in database in application.properties file. The default url: jdbc:postgresql://localhost:5432/n11gradproject and password:123**

